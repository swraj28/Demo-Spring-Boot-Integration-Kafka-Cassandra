<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
    <Properties>
        <Property name="springAppName">${bundle:application:spring.application.name}</Property>
        <Property name="sleuthInfo">${springAppName},%X{X-B3-TraceId}</Property>
    </Properties>

    <Appenders>
        <File name="RollingFile"
              fileName="/var/log/smsParsingCCTransactions/smsParsingCCTransactions.log">
            <PatternLayout>
                <pattern>%style{%d{ISO8601}}{black} %highlight{%-5level }[${sleuthInfo}][%style{%t}{bright,blue}] %style{%C{1.}}{bright,red}: %msg%n%throwable</pattern>
                <disableAnsi>false</disableAnsi>
            </PatternLayout>
        </File>
    </Appenders>

    <Loggers>
        <!-- LOG everything at INFO level -->
        <Root level="info">
            <AppenderRef ref="RollingFile"/>
        </Root>

        <!-- LOG "com.example.demo.listeners.smsParsingCCTransactions" at INFO level -->
        <Logger name="com.example.demo" level="info"/>
    </Loggers>
</Configuration>